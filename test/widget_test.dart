// 파일 목적: 기본 위젯 테스트 (flutter create .로 자동 생성, 앱에 맞게 수정)
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    // 앱 통합 테스트는 Hive 초기화가 필요하므로 별도 test/ 파일에서 관리
    expect(true, isTrue);
  });
}
